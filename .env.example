# Anthropic API key — server-side only, never exposed to the browser.
# Get one at https://console.anthropic.com (and set a spend limit there).
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the extraction model (default: claude-haiku-4-5-20251001)
# CLAUDE_MODEL=claude-haiku-4-5-20251001
